## Packages
framer-motion | Complex animations and page transitions
recharts | Visualization for tool usage statistics
date-fns | Date manipulation for Age Calculator and other tools
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes

## Notes
The app tracks tool usage via POST /api/stats/:toolId/view
Sidebar should be responsive (collapsible on mobile)
Dark mode support is required via Tailwind 'dark' class
